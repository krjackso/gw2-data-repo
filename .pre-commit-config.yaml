repos:
  - repo: local
    hooks:
      - id: validate-item-yaml
        name: Validate item YAML files
        entry: uv run python -m scripts.validate
        language: system
        pass_filenames: false
        files: ^data/items/.*\.yaml$
